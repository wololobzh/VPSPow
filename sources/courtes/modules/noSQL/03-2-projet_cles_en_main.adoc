:titre: Projet clé en main avec MongoDB et Node.js
:module: NoSQL
:duree: 60
:description: Présentation d'un projet clé en main avec Node.js, Express et MongoDB, incluant un CRUD complet pour la ressource users.
include::../../../../adoc/libs/header-module.adoc[]

ifeval::["{visible-on-html}" !=  "yes"]
:docinfo: shared
:docinfodir: ../../../adoc/libs
endif::[]

== Introduction à MongoDB

== Objectifs pédagogiques
// tag::objectifs[]
* Avoir un projet clé en main avec un CRUD complet pour la ressource users.
// end::objectifs[]

// tag::deroulement[]
== Introduction 

Présentation d'un projet clé en main avec Node.js, Express et MongoDB

== Contenu

* **Node.js + Express**
* **MongoDB (docker)**
* **Mongo Express (GUI web)**
* **Database Tools**
* **Seed initial**
* **Code propre, organisé, modulaire**

[NOTE.speaker]
-- 
**Ce projet clé en main** avec un **CRUD complet** pour la ressource `users`, te permettra de démarrer rapidement avec une API RESTful utilisant MongoDB comme base de données.
-- 

== Lancer le projet

Le projet sera prêt à tourner uniquement avec :

```
docker compose up --build -d
```

Rien d’autre à faire.

== Structure du projet

```
/mongo-node-crud
│
├─ docker-compose.yml
├─ dockerfiles/
│   └─ tools/
│       ├─ Dockerfile
│       └─ scripts/
│           └─ wait-for-mongo.sh
│
├─ init/
│   └─ seed.js
│
├─ api/
│   ├─ package.json
│   ├─ .env
│   ├─ server.js
│   ├─ db.js
│   └─ routes/
│       └─ users.js
│
└─ data/
```

---

== docker-compose.yml

`/mongo-node-crud/docker-compose.yml`

```yaml
version: "3.8"

services:
  mongodb:
    image: mongo:7
    container_name: mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: root
      MONGO_INITDB_ROOT_PASSWORD: example
    ports:
      - "27017:27017"
    volumes:
      - ./data:/data/db
      - ./init:/docker-entrypoint-initdb.d:ro

  mongo-express:
    image: mongo-express:1.0.0
    container_name: mongo-express
    depends_on:
      - mongodb
    restart: unless-stopped
    ports:
      - "8081:8081"
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: root
      ME_CONFIG_MONGODB_ADMINPASSWORD: example
      ME_CONFIG_MONGODB_SERVER: mongodb

  api:
    build: ./api
    container_name: node-api
    restart: unless-stopped
    ports:
      - "3000:3000"
    env_file:
      - ./api/.env
    depends_on:
      - mongodb

  tools:
    build:
      context: ./dockerfiles/tools
    container_name: mongo-tools
    tty: true
    stdin_open: true
```

== Dockerfile Mongo Tools**

`/mongo-node-crud/dockerfiles/tools/Dockerfile`

```dockerfile
FROM mongo:7

RUN apt-get update && apt-get install -y bash curl nano \
  && rm -rf /var/lib/apt/lists/*

WORKDIR /work
CMD ["/bin/bash"]
```

== Script seed MongoDB

`/mongo-node-crud/init/seed.js`

```js
db = db.getSiblingDB("testdb");

db.users.insertMany([
  { name: "Alice", age: 30, email: "alice@test.com" },
  { name: "Bob", age: 35, email: "bob@test.com" }
]);
```

---

== API Node.js (Express)

== package.json

`/mongo-node-crud/api/package.json`

```json
{
  "name": "node-mongo-crud",
  "version": "1.0.0",
  "main": "server.js",
  "type": "module",
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.19.2",
    "mongodb": "^6.7.0"
  }
}
```

---

== `.env`

`/mongo-node-crud/api/.env`

```env
PORT=3000
MONGO_URI=mongodb://root:example@mongodb:27017/testdb?authSource=admin
```

---

== db.js

`/mongo-node-crud/api/db.js`

```js
import { MongoClient } from "mongodb";
import dotenv from "dotenv";
dotenv.config();

const client = new MongoClient(process.env.MONGO_URI);

let db;

export async function connectDB() {
  try {
    await client.connect();
    db = client.db();
    console.log("✅ Connected to MongoDB");
  } catch (err) {
    console.error("❌ DB connection error:", err);
  }
}

export function getDB() {
  return db;
}
```

---

== CRUD complet – users route

`/mongo-node-crud/api/routes/users.js`

```js
import { Router } from "express";
import { ObjectId } from "mongodb";
import { getDB } from "../db.js";

const router = Router();

/**
 * GET /users
 */
router.get("/", async (req, res) => {
  const users = await getDB().collection("users").find().toArray();
  res.json(users);
});

/**
 * GET /users/:id
 */
router.get("/:id", async (req, res) => {
  try {
    const user = await getDB()
      .collection("users")
      .findOne({ _id: new ObjectId(req.params.id) });

    if (!user) return res.status(404).json({ error: "User not found" });

    res.json(user);
  } catch (err) {
    res.status(400).json({ error: "Invalid ID" });
  }
});

/**
 * POST /users
 */
router.post("/", async (req, res) => {
  const result = await getDB().collection("users").insertOne(req.body);
  res.status(201).json(result);
});

/**
 * PUT /users/:id
 */
router.put("/:id", async (req, res) => {
  try {
    const result = await getDB().collection("users").findOneAndUpdate(
      { _id: new ObjectId(req.params.id) },
      { $set: req.body },
      { returnDocument: "after" }
    );

    if (!result.value) return res.status(404).json({ error: "User not found" });

    res.json(result.value);
  } catch (err) {
    res.status(400).json({ error: "Invalid ID" });
  }
});

/**
 * DELETE /users/:id
 */
router.delete("/:id", async (req, res) => {
  try {
    const result = await getDB()
      .collection("users")
      .deleteOne({ _id: new ObjectId(req.params.id) });

    if (result.deletedCount === 0)
      return res.status(404).json({ error: "User not found" });

    res.json({ success: true });
  } catch (err) {
    res.status(400).json({ error: "Invalid ID" });
  }
});

export default router;
```

---

== server.js

`/mongo-node-crud/api/server.js`

```js
import express from "express";
import dotenv from "dotenv";
import { connectDB } from "./db.js";
import usersRouter from "./routes/users.js";

dotenv.config();

const app = express();
app.use(express.json());

// routes
app.use("/users", usersRouter);

app.listen(process.env.PORT, () => {
  console.log(`🚀 API running at http://localhost:${process.env.PORT}`);
  connectDB();
});
```

---

== Lancer le projet**

Depuis `/mongo-node-crud` :

```
docker compose up --build -d
```

---

== Tester le CRUD**

== Lister les users

```
GET http://localhost:3000/users
```

== Récupérer un user

```
GET http://localhost:3000/users/:id
```

== Ajouter un user

```
POST http://localhost:3000/users
Content-Type: application/json

{
  "name": "Charlie",
  "age": 28,
  "email": "charlie@test.com"
}
```

== Modifier un user

```
PUT http://localhost:3000/users/:id
```

== Supprimer un user

```
DELETE http://localhost:3000/users/:id
```

== Bonus – Mongo Express

👉 [http://localhost:8081](http://localhost:8081)

Tu peux y visualiser `testdb > users`.

// end::deroulement[]

== Conclusion

// tag::conclusion[]
Ce projet clé en main avec un CRUD complet pour la ressource `users` te permettra de démarrer rapidement avec une API RESTful utilisant MongoDB comme base de données. N'hésite pas à l'adapter et à l'étendre selon tes besoins spécifiques !
// end::conclusion[]

